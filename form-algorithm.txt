Проверить, что отправлена форма;

Получить все данные из формы;

Написать функции для проверки заполненности и валидации поля по формату;
В цикле обойти все поля формы и применить к ним эти функции;
Выполнить все проверки на соотвествие формату:

        Проверка даты
             - Содержимое поля «дата завершения» должно быть датой в формате «ГГГГ-ММ-ДД»;
             - Эта дата должна быть больше или равна текущей.

        Проверка проекта
             - Для идентификатора выбранного проекта проверять, что он ссылается на реально существующий проект.

        Проверка имени задачи
             - Имя задачи не должно быть пустой строкой.

Если ошибки:
      - проект НЕ сохраняем;
      - формируем тексты ошибок (например, Имя проекта не должно превышать *** символов);
      - отображаем форму, но уже не с пустыми полями, а с введенными до выполнения запроса значениями;
      - тексты ошибок отображаем рядом с полями.

Если ошибок нет:
     - Сохранить новую задачу (учитывая выбранный проект), затем переадресовать пользователя на главную страницу;
     - Если к задаче был прикреплен файл, то перенести его в публичную директорию и сохранить ссылку на главной странице
